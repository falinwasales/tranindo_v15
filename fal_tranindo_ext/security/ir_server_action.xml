<odoo>
    <record id="server_action_update_sales_order_name" model="ir.actions.server">
        <field name="name">Update Sales Order Reference</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code">for sale in records:
    if sale.state not in ('done', 'sale'):
        sale.update({"name": "SQ%s" % (sale.name[3:])})
        </field>
    </record>

    <record id="server_action_get_product_category" model="ir.actions.server">
        <field name="name">Get Product Category</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="state">code</field>
        <field name="code">
            for self in records:
                if self.move_type == 'out_invoice' and self.invoice_line_ids:
                    for lines in self.invoice_line_ids:
                        self.update({'category_first_line': lines[0].product_id.categ_id.id})
        </field>
    </record>
</odoo>